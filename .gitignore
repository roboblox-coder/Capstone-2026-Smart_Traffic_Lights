# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Local tooling / editor / session artifacts (never source)
.claude/
GRAPHIFY.md
graphify-out/
graphify-out-*/
.graphify/

# Experiment outputs: checkpoints, logs, scratch. Deliverable models are
# promoted into SUMO/v2/ai/ paths deliberately (e.g. ai/v3/model_*.pth);
# everything generated under runs/ stays local.
SUMO/v2/ai/runs/
SUMO/v2/*.log
SUMO/v2/e2output.xml
